[pytest]
filterwarnings =
    ignore:.*torch\.jit\.script.*is deprecated.*:FutureWarning
norecursedirs = .git .venv venv
python_files = test_*.py
python_paths = .
# Root test_agent.py is a manual script, not a pytest suite
addopts = --ignore=test_agent.py
